# Write your MySQL query statement below
select p.product_id,round(ifnull(sum(u.units*p.price)/sum(u.units),0),2) as average_price from Prices p  left join UnitsSold u on u.product_id=p.product_id && u.purchase_date >=p.start_date and u.purchase_date<=p.end_date group by p.product_id;